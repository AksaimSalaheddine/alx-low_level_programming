#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char message[] = "\"Programming is like building a multilingual puzzles";

	puts(message);
	/** return */
	return (0);
}
